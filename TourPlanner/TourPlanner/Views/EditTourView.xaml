<UserControl x:Class="TourPlanner.Views.EditTourView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.Views"
             xmlns:converters="clr-namespace:TourPlanner.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <converters:RadioButtonCheckedConverter x:Key="RadioButtonCheckedConverter" />

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Style.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
        
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBox  x:Name="tourName" Margin="10" Text="{Binding TourName}" Style="{StaticResource AddTourInput}"/>
        <TextBlock Text="{Binding  SelectedTour.Name}" Style="{StaticResource AddTourName}"/>

        <TextBox x:Name="tourDescription" Grid.Row="0" Grid.RowSpan="4" Grid.Column="1" Margin="10" Text="{Binding TourDescription}" Style="{StaticResource AddTourInput}"/>
        <TextBlock Text="{Binding  SelectedTour.Description}" Grid.Row="0" Grid.Column="1" Margin="10" Style="{StaticResource AddTourDescription}"/>

        <TextBox x:Name="tourFrom" Grid.Row="2" Margin="10" Text="{Binding TourFrom}" Style="{StaticResource AddTourInput}"/>
        <TextBlock Text="{Binding  SelectedTour.FromLocation}" Grid.Row="2" Margin="10" Style="{StaticResource AddTourFrom}"/>

        <TextBox x:Name="tourTo" Grid.Row="3" Margin="10" Text="{Binding TourTo}" Style="{StaticResource AddTourInput}"/>
        <TextBlock Text="{Binding  SelectedTour.ToLocation}" Grid.Row="3" Margin="10" Style="{StaticResource AddTourTo}"/>

        <StackPanel HorizontalAlignment="Center" Grid.Row="4" Orientation="Horizontal" Margin="10">
            <RadioButton Style="{StaticResource AddTourCar}" GroupName="TransportGroup" IsChecked="{Binding SelectedTour.TransportType, Converter={StaticResource RadioButtonCheckedConverter}, ConverterParameter=CAR}" Tag="CAR" />
            <RadioButton Style="{StaticResource AddTourBicycle}" GroupName="TransportGroup" IsChecked="{Binding SelectedTour.TransportType, Converter={StaticResource RadioButtonCheckedConverter}, ConverterParameter=BICYCLE}" Tag="BICYCLE" />
            <RadioButton Style="{StaticResource AddTourWalking}" GroupName="TransportGroup" IsChecked="{Binding SelectedTour.TransportType, Converter={StaticResource RadioButtonCheckedConverter}, ConverterParameter=WALKING}" Tag="WALKING" />
        </StackPanel>


        <Grid Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="Generate Map" Margin="10" Command="{Binding GenerateMapCommand}"/>
            <Button Grid.Column="1" Content="Edit Tour" Margin="10" Command="{Binding EditCommand}"/>

        </Grid>

    </Grid>

</UserControl>
